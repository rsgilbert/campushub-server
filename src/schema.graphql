type Query {
    test: String!
    parks: [Park!]!
    park(id: ID!): Park
}

type Park {
    id: ID!
    name: String
    manager: Manager
    location: Location
}

type Manager {
    id: ID!
    name: String
}

type Location {
    id: ID!
    place: String
}
